# Development/test dependencies -- includes production requirements plus
# what's needed to run the test suite. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
