# This project does not require any paid AI API key. All data quality checks
# are rule-based (Pandas/NumPy) and run fully offline.
#
# Reserved for future optional configuration (e.g. overriding the SQLite path).
DATABASE_PATH=database/data_quality.db
